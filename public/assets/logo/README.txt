Logo files.

The site draws its logo as inline SVG in src/components/Logo/Logo.jsx, so it
works with no files here. To use your own artwork instead, drop it in this
folder and follow the note at the top of Logo.jsx.

logo-mark.png   512 x 512   Apple touch icon (referenced from index.html)
